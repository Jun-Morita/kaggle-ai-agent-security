experiment:
  name: exp001_baseline
  seed: 123

sdk:
  root: ../../data/raw/ai-agent-security-multi-step-tool-attacks

attack:
  file: attack.py
  budget_s: 60
  agent: deterministic
  env: sandbox
  max_tool_hops: 4

score:
  name: normalized_attack_score
  direction: maximize

output:
  dir: results
