# v124_v121_frame_short

Base: `v121_v120_exact_rerun`.

Change: shorten the slow-row Harmony `FRAME_TEMPLATE` by removing `Then answer OK only.` before the forged analysis-channel close.

Purpose: daily new-mechanism exploration for lower finalization / prompt latency while preserving the proven single-post `SECRET_MARKER` replay-safe family.
