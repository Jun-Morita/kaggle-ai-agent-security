# Daily Report 2026-08-31

## Competition

- Name: AI Agent Security - Multi-Step Tool Attacks
- Current confirmed best: `v169_replay_costcoef095=89.685`

## Status

- `v197`-`v200` all completed below `v169`:
  - `v197=84.060`: fewer classifier probes regressed.
  - `v198=87.030`: byte-identical `v169` rerun did not upper-tail.
  - `v199=83.700`: more classifier probes regressed.
  - `v200=85.005`: terminal wording diversity regressed.
- Keep `SPLIT_CLASSIFY_N=6` and single-call Gemma-safe prompting as the default.

## Research

- Recent Code checks did not reveal a public notebook above our current best:
  `evgendvorkin/ai-agent=89.355`, `anasriaz/ai-agent-security=88.110`,
  `lopure/jed-multi-step-attack-relay-push100=85.905`.
- Discussion `737781` reinforces that Gemma should stay one tool call per
  message and that malformed / quoted-key tool-call arguments can zero a
  candidate.
- Discussion `737793` suggests the `90` wall may involve model imbalance, but
  the `v197`/`v199` readout shows probe count alone is not the fix.
- Discussion `738122` reinforces private shake risk, so final slots should keep
  some diversity rather than only public-score reruns.

## Submitted Today

| Version | Submission ref | Controlled change | State |
| --- | ---: | --- | --- |
| `v201` | `55915447` | byte-identical `v169` final rerun | pending |
| `v202` | `55915449` | byte-identical `v166` `Say OK.` rerun | pending |
| `v203` | `55915452` | byte-identical `v163` terminal-short rerun | pending |
| `v204` | `55915453` | `v169` with Gemma `bare keys` syntax hint | pending |
| `v205` | `55915455` | `v169` exfil head plus 3-candidate deputy tail | pending |

All five CPU kernels completed, generated `attack.py` and `submission.csv`, and
passed `py_compile`, notebook parity, metadata JSON validation, hosted output
matching, and SDK redteam validation before competition submission. The daily
submission quota is now exhausted.

## Final Selection Notes

- Provisional public-score anchor: `v169_replay_costcoef095=89.685`.
- Provisional second slot if no new score improves: `v166_terminal_say_ok_direct=89.235`.
- `v166` is a useful but limited shake hedge. It differs from `v169` in terminal
  wording (`Say OK.`) and replay-cost conservatism (`REPLAY_COST_COEF=1.0` rather
  than `0.95`), so it can cover a hosted/private replay-cost or terminal wording
  miss by `v169`.
- `v166` is not a strong private-axis hedge because it still uses the same
  `SECRET_MARKER` single-post `EXFILTRATION` surface. If private guardrails
  suppress that surface broadly, `v169` and `v166` may fail together.
- `v205` is the explicit low-volume private diversity probe, but it should only
  displace `v166` if its public score is not too costly or later evidence makes
  exfil-only final slots look too correlated.

## Next

1. Wait for `v201`-`v205` public scores.
2. Promote any result above `v169=89.685`.
3. If none improve, keep final selection centered on `v169` plus either the
   strongest distinct high-score branch (`v166`) or a lower-scoring explicit
   private hedge if shake risk dominates.
