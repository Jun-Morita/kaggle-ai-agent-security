# Daily Report 2026-08-21

## Competition

- Name: AI Agent Security - Multi-Step Tool Attacks
- Current date: 2026-08-21
- Final submission deadline: 2026-09-01 23:59 UTC
- Current scoring regime: post 2026-08-05 evaluator / leaderboard refresh
- Current confirmed post-refresh best: `v148_replaysafe_postrefresh_push=88.245`

## Today

- Checked Kaggle submissions and confirmed the 2026-08-20 CONFUSED_DEPUTY
  experiments:
  - `v156_confdeputy_compound`: `49.390`
  - `v157_confdeputy_only`: `20.715`
  - `v158_confdeputy_twoturn`: complete with blank public score
- Downloaded the latest public leaderboard CSV:
  - `3822` teams
  - Our team: rank `619`, score `88.245`
  - top-5% boundary: rank `192`, score `91.575`
  - top-10% boundary: rank `383`, score `89.415`
- Ingested latest public Code and Discussion caches via the Kaggle skill:
  - Code ingest: `2` new, `98` updated.
  - Discussion ingest: `33` new, `67` updated, `465` comments across
    `100` discussions.
- Tried fetching enriched public Code scores, but Kaggle returned API rate
  limit `429`; do not retry in a tight loop.

## Findings

- `v156=49.390` confirms the source-level predicate analysis was correct but
  the one-message compound action construction is not viable: adding even a
  guardrail-allowed `email.send` clause interferes too much with the proven
  `http.post` path.
- `v157=20.715` confirms CONFUSED_DEPUTY can score, but its severity and
  throughput are too low for public Silver. Keep it only as a private-risk
  hedge or diagnostic.
- `v158` blank confirms the two-turn warm-up structure is not worth the extra
  replay cost in this form.
- The active public-score path remains `v148`: single-action, single-message,
  live-validated `SECRET_MARKER` exfil with `REPLAY_SAFE_FRAC=0.985`.
- Discussion `736246` / `736099` adds two useful operational points:
  - hosted evaluation appears CPU-like / unstable regardless of notebook GPU
    setting;
  - one participant reports that returning many candidates may only score the
    leading `200-250`, which supports prioritizing high-success, low-latency
    candidates early rather than blindly increasing returned count.

## Current Position

- Confirmed best: `v148=88.245`.
- Silver/top-5% boundary: `91.575`.
- Gap: `+3.330`, about `37` additional successful single-post EXFILTRATION
  candidates if the `0.09` points-per-firing-candidate model holds.

## Next

- Return to the `v148` family. The next viable public-score attempt should
  improve the first scored slice's success density or latency, not add a second
  action or a second turn.
- Re-check high-score public Code after the Kaggle API rate limit cools down.
  Prioritize genuinely new source hashes before spending submission slots.
- Consider a small `v148`-line ordering / early-candidate optimization: if
  replay effectively scores only the front of the returned list, ensure the
  fastest, most reliable template branch appears first.

## Prepared Experiments

- `v159_fastest_first_rsf985`: submitted ref `55669483`, pending. Fork of `v148` that keeps the same live
  validation-fill and `REPLAY_SAFE_FRAC=0.985`, but returns fired candidates
  sorted by live elapsed time. This directly tests the "first scored slice"
  hypothesis with a minimal behavioral change.
- `v160_probe_only_fastest_first`: submitted ref `55669526`, pending. Higher-risk companion that uses the template
  race only for classification, does not return classification probes, and sorts
  post-classification fired candidates fastest-first. This sacrifices a few early
  fired probes to make the replay prefix more homogeneous.
- `v161_v148_exact_rerun`: submitted ref `55669672`, pending. Byte-equivalent
  rerun of the current best `v148=88.245` to sample hosted variance / upper-tail
  behavior.
- `v162_fastest_first_rsf9875`: submitted ref `55669708`, pending. Keeps
  fastest-first ordering and pushes `REPLAY_SAFE_FRAC` to the midpoint `0.9875`
  between the current best `0.985` and the failed `0.99` test.
- All four candidates passed `py_compile`, notebook/`attack.py` consistency checks,
  and SDK `validate redteam`.
