# Daily Report 2026-06-26

## Competition

- Name: AI Agent Security - Multi-Step Tool Attacks
- Metric: normalized attack score
- Historical best public LB: `54.000` from pre-update `v030_replay_dense_classic600`
- Current confirmed post-update public baseline: `9.960` from `v038_jed_attack_improved_nb`

## Today

- Kaggle submission status を確認した。
- `v038_jed_attack_improved_nb` は `SubmissionStatus.COMPLETE`、public `9.960`。
- `v040_jed_marker400` は `SubmissionStatus.COMPLETE` だが public score は空欄。no-score / replay timeout として扱う。
- `v039_replay_dense_classic600_rerun` は public `0.540` のまま。旧 `classic600` は更新後 evaluator で再現しない。
- 公開 notebook `omega attack v9 0 secret marker n250` を確認した。public score は `22.825`。
- Omega は `SECRET_MARKER` payload、`250` exfil candidates、4種類の `http.post` phrase allocation、5件の confused-deputy probe で構成される。
- `22.825` は `250 * 18 / 200 = 22.5` に近く、post-update では `SECRET_MARKER` single-post scaling が主経路になっている。
- `v041_omega_secret_marker_n250` を作成・提出した。
- `v041` は Omega の `250` exfil candidates + `5` confused-deputy probes を維持し、repo の標準 script wrapper を追加した。
- `v041` の py_compile、ruff、SDK validate、deterministic smoke は通過。deterministic smoke は約14.4s、score `0.00`。static return count は255。
- `v041` は submission ref `54075682`、status `COMPLETE`、public `22.545`。
- 追加の上側境界 probe として `v042_omega_marker300` を作成・提出した。
- `v042` は Omega の phrase ratio を維持しつつ `250 -> 300` exfil candidates に増やし、5件の confused-deputy probe は維持した。
- `v042` の py_compile、ruff、SDK validate、deterministic smoke は通過。deterministic smoke は約54.4s、score `0.00`。static return count は305。
- `v042` は submission ref `54075852`、status `COMPLETE`、public `27.000`。

## Results

| Version | Public LB | Status | Notes |
|---|---:|---|---|
| `v042_omega_marker300` | 27.000 | complete | 300 exfil candidates scored exactly as expected |
| `v040_jed_marker400` | none | complete / no-score | direct JED scale to 400 exceeded replay boundary |
| `v039_replay_dense_classic600_rerun` | 0.540 | complete | exact v030 rerun; old classic600 no longer reproduces |
| `v038_jed_attack_improved_nb` | 9.960 | complete | confirmed post-update `SECRET_MARKER` baseline |
| `v041_omega_secret_marker_n250` | 22.545 | complete | close port of public Omega N250; near 250-exfil baseline |

## Findings

- `v038` confirms that `SECRET_MARKER` candidates score under the updated evaluator.
- `v040` shows that `400` candidates is too large in our current JED wrapper.
- Public Omega `N=250` is the best available next target because it lands between known-good `N=100` and no-score `N=400`.
- `v041` and `v042` show that `SECRET_MARKER` single-post scoring scales linearly at least through 300 candidates.
- `v042` score exactly equals `300 * 18 / 200`; confused-deputy probes did not materially increase public score.

## Decisions

- Pivot away from old classic replay-dense scaling for current evaluator submissions.
- Treat `N=250` multi-phrase `SECRET_MARKER` as the next practical submission candidate.
- Preserve Omega phrase counts on the first port; do not add count scaling until it transfers.
- `v041` is the active pending Omega transfer.
- `v042` becomes the current confirmed post-update best.
- The next useful public-boundary experiments are between `300` and `400`, with `320-360` as the plausible range.

## Next

- [x] Check `v038/v040` hosted results.
- [x] Review `omega-attack-v9-0-secret-marker-n250.ipynb`.
- [x] Build and validate `v041_omega_secret_marker_n250`.
- [x] Submit `v041_omega_secret_marker_n250`.
- [x] Check `v041` result.
- [x] Submit `v042_omega_marker300`.
- [x] Check `v042` result.
