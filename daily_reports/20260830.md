# Daily Report 2026-08-30

## Competition

- Name: AI Agent Security - Multi-Step Tool Attacks
- Current confirmed best: `v169_replay_costcoef095=89.685`

## Status

- Public leaderboard snapshot: `4182` scored teams; our team is rank `533` at
  `89.685`.
- top-10% boundary: rank `419`, score `90.585` (gap `+0.900`).
- top-5% proxy: rank `210`, score `94.095` (gap `+4.410`).
- Recent completed readouts:
  - `v194=84.020`: slow-row multipost regressed.
  - `v195=85.605`: small deputy tail regressed.
  - `v196=82.170`: safer replay-cost coefficient regressed.

## Research

- Recent public code remained below our best in direct score checks:
  `evgendvorkin/ai-agent=89.355`, `anasriaz/ai-agent-security=88.110`,
  `lopure/jed-multi-step-attack-relay-push100=85.905`.
- Discussion `737781` indicates Gemma still fails on multiple tool calls in one
  generation, so single-call prompt shapes remain the primary public-score path.
- Discussion `737793` suggests scores around `90` may be model-imbalanced, so
  split-classification width is worth testing.

## Submitted Today

| Version | Submission ref | Controlled change | State |
| --- | ---: | --- | --- |
| `v197` | `55878839` | `SPLIT_CLASSIFY_N=4` throughput readout | pending |
| `v198` | `55878845` | byte-identical `v169` CPU rerun | pending |
| `v199` | `55878857` | `SPLIT_CLASSIFY_N=8` balance readout | pending |
| `v200` | `55878860` | terminal wording diversity: `Then answer OK only.` | pending |

All four CPU kernels completed, generated `attack.py` and `submission.csv`, and
passed `py_compile`, notebook parity, metadata JSON validation, hosted output
matching, and SDK redteam validation before competition submission.

## Next

1. Wait for `v197`-`v200` public scores.
2. Promote only a result above `v169=89.685`; otherwise keep `v169` as the final
   public fallback.
3. If `v197` beats `v199`, continue toward throughput and fewer probes. If
   `v199` beats `v197`, continue toward model-balance routing.

