# Daily Report 2026-06-27

## Competition

- Name: AI Agent Security - Multi-Step Tool Attacks
- Metric: normalized attack score
- Historical best public LB: `54.000` from pre-update `v030_replay_dense_classic600`
- Current confirmed post-update public best: `30.170` from `v043_omega_marker330`

## Today

- Kaggle submission status を確認した。
- `v041_omega_secret_marker_n250` は `SubmissionStatus.COMPLETE`、public `22.545`。
- `v042_omega_marker300` は `SubmissionStatus.COMPLETE`、public `27.000`。
- `v043_omega_marker330` は `SubmissionStatus.COMPLETE`、public `30.170`。
- `v044_omega_marker350` は `SubmissionStatus.COMPLETE` だが public score は空欄。no-score / replay timeout として扱う。
- `v045_omega_multipost2_n120` は `SubmissionStatus.COMPLETE` だが public score は空欄。no-score / replay timeout として扱う。
- `v046_omega_marker340_pure` は `SubmissionStatus.COMPLETE` だが public score は空欄。no-score / replay timeout として扱う。
- `v047_encoded_marker_n160` を提出した。submission ref は `54111527`、現時点では pending。
- `v048_private_robust_mixed` を提出した。submission ref は `54111548`、現時点では pending。

## Results

| Version | Public LB | Status | Notes |
|---|---:|---|---|
| `v048_private_robust_mixed` | pending | pending | 220 `sk_live_test` + 30 encoded marker + 5 deputy |
| `v047_encoded_marker_n160` | pending | pending | encoded `SECRET_MARKER` evaluator-update probe |
| `v046_omega_marker340_pure` | none | complete / no-score | 340 pure exfil exceeded current wrapper boundary |
| `v045_omega_multipost2_n120` | none | complete / no-score | multipost2 probe did not produce a score |
| `v044_omega_marker350` | none | complete / no-score | 350 exfil exceeded replay boundary |
| `v043_omega_marker330` | 30.170 | complete | current post-update best; above expected `29.7` |
| `v042_omega_marker300` | 27.000 | complete | current post-update best; exactly `300 * 18 / 200` |
| `v041_omega_secret_marker_n250` | 22.545 | complete | near `250 * 18 / 200 = 22.5` |
| `v040_jed_marker400` | none | complete / no-score | 400 candidates exceeded replay boundary |
| `v038_jed_attack_improved_nb` | 9.960 | complete | 100-candidate SECRET_MARKER baseline |

## Findings

- Post-update scoring is now dominated by `SECRET_MARKER` single-post exfiltration.
- Linear scale is confirmed from `N=100` to `N=300`.
- `N=350` and `N=400` no-scored in our wrapper, so the practical replay boundary is between 330 and 350.
- Confused-deputy probes did not materially affect public score in `v041/v042`.
- `v045` は no-score のため、multipost レバーを復活させる根拠にはならなかった。
- `v046` は deputy を外しても pure `N=340` が no-score。現在の実用境界は `v043` の total `335` 以下と見る。
- `v047/v048` は count-boundary ではなく、encoded exfil と private-robust hedge の別軸として提出した。

## Decisions

- Treat `v043` as the active post-update best.
- For public LB, continue with Omega-style `SECRET_MARKER` count-boundary probes.
- `v043` を active post-update best として維持する。
- 次に刻むなら `N=335` pure exfil が妥当。`N=340` は超過。
- 刻みよりも、encoded exfil と private-robust mixed の結果を確認して次の軸を決める。
- For private robustness, keep a separate hedge; current public-best path is still a narrow sentinel-exfil trick.

## Next

- [ ] Consider `N=330` or `N=350` Omega-style probe.
- [x] Submit `v043_omega_marker330`.
- [x] Submit `v044_omega_marker350`.
- [x] Check `v043/v044` results.
- [x] Submit `v045_omega_multipost2_n120`.
- [x] Submit `v046_omega_marker340_pure`.
- [x] Check `v045` result.
- [x] Check `v046` result.
- [x] Submit `v047_encoded_marker_n160`.
- [x] Submit `v048_private_robust_mixed`.
- [ ] Check `v047` result.
- [ ] Check `v048` result.
- [ ] Consider `N=335` or `N=340` Omega-style probe.
- [ ] Consider a private-robust variant that replaces some public-only exfil with safer payload or mixed predicates.
