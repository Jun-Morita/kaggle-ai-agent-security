# Daily Report 2026-07-26

## Competition

- Name: AI Agent Security - Multi-Step Tool Attacks
- Deadline: 2026-09-01 final submission deadline
- Metric: normalized attack score
- Current best local replay: deterministic smoke remains `0.00` and is used only for wiring
- Current best public LB: `88.605` from `v110_kaiwalya_classify6`
- Current best private LB: not visible

## Today

- Confirmed `v110_kaiwalya_classify6` completed with public `88.605`, becoming the new repo best.
- Confirmed `v111_probehops1_coef18` completed with Kaggle `incorrect format` / blank public score.
- Confirmed `v108_kaiwalya_v9_rerun` completed with public `87.615`.
- Downloaded the public leaderboard. Current rank is `163 / 2386`; top 5% boundary is `89.235`, top 10% boundary is `87.840`.
- Re-ingested Kaggle Code and Discussion caches using the NVIDIA Kaggle skill.
- Checked recent public Code candidates. `dimong4/ai-agent-security` best version is `88.200`, `nikitagajbhiye30/ai-security-0011` best version is `87.120`, and neither exceeds `v110`.
- Reviewed discussion evidence around throughput and format errors. `Submission Format Error` can be timeout / evaluator-envelope failure rather than a real CSV schema defect.
- Submitted `v112_classify6_rerun`, an exact rerun of `v110` to sample hosted replay variance.
- Submitted `v113_classify5_boundary`, a one-knob boundary test changing only `SPLIT_CLASSIFY_N=6 -> 5`.
- Confirmed the current Kaggle code-submission API rejects `-f attack.py`; successful submissions used `-f submission.csv` while the kernel output still contains `attack.py`.

## Experiments

| Experiment | Hypothesis | Local score | Public LB | Status | Notes |
|---|---|---:|---:|---|---|
| `v113_classify5_boundary` | Reducing classification probes one more step can recover latency without crossing the failure boundary seen at `4`. | `0.00` | pending | submitted | Ref `54992805`; only attack change is `SPLIT_CLASSIFY_N=6 -> 5`; notebook JSON, py_compile, and SDK validate passed. |
| `v112_classify6_rerun` | Exact rerun of `v110=88.605` can land in the hosted upper tail and close the `0.630` Silver gap. | `0.00` | pending | submitted | Ref `54992796`; attack hash matches `v110` (`ff40267e841a`). |
| `v111_probehops1_coef18` | Probe at one hop and charge replay cost with coefficient `1.8` to improve fill throughput. | `0.00` | blank | complete | Kaggle `incorrect format`; negative evidence for adding probe hops in this notebook envelope. |
| `v110_kaiwalya_classify6` | A middle-rung classification reduction can improve throughput while avoiding `v109` failure. | `0.00` | `88.605` | complete | New repo best; only change from `v106` was `SPLIT_CLASSIFY_N=8 -> 6`. |
| `v109_kaiwalya_classify4` | More aggressive classification reduction can recover additional interactions. | `0.00` | blank | complete | Kaggle `incorrect format`; `4` appears too aggressive. |
| `v108_kaiwalya_v9_rerun` | Exact rerun of `v106` can sample hosted replay variance. | `0.00` | `87.615` | complete | Underperformed `v106=88.110`, confirming replay variance. |

## Findings

- The current Silver gap is small: `89.235 - 88.605 = 0.630`, about seven clean single-post findings.
- `SPLIT_CLASSIFY_N=6` is currently the best confirmed latency knob. `8 -> 6` improved `v106=88.110` to `v110=88.605`; `4` failed.
- `PROBE_HOPS=1` with `REPLAY_COST_COEF=1.8` failed as `v111`, consistent with discussion reports that format errors often mean evaluator timeout or incomplete replay.
- Exact reruns still matter: `v108` underperformed, but discussion `728947` reports large byte-identical replay variance, so `v112` is a rational Silver attempt.
- Public Code checked today did not reveal a clearly better immediately transferable source than the current `v110` family.
- The highest useful public strategy remains single-post `SECRET_MARKER` exfiltration with replay-safe live validation-fill and short prompt latency.

## Replay / Failure Notes

- `kaggle competitions submit -f attack.py` now fails with `Submission files must be named "submission.csv"`.
- The correct CLI path is to submit a completed kernel version with `-f submission.csv`; the notebook must still write `/kaggle/working/attack.py`.
- `v112` and `v113` kernel outputs both contained `attack.py` and `submission.csv`, and both competition submissions were accepted as pending after switching to `submission.csv`.
- `v111` local syntax and SDK validation passed, so its blank result should be treated as replay/evaluator-envelope failure, not an import or class-definition defect.

## Knowledge / References

- `submit/v110_kaiwalya_classify6`
- `submit/v112_classify6_rerun`
- `submit/v113_classify5_boundary`
- Kaggle Discussion `728947`: throughput identity and replay variance
- Kaggle Discussion `725073`: format errors may be timeout / incomplete evaluation
- Kaggle Discussion `729548`: throughput remains the likely public meta
- Public Code checks: `dimong4/ai-agent-security`, `nikitagajbhiye30/ai-security-0011`, `tiktoktrendz/jed-submission`

## Decisions

- Promote `v110=88.605` to the current confirmed baseline.
- Treat `v112` as the low-risk Silver attempt.
- Treat `v113` as the controlled upside pair.
- Do not retry `PROBE_HOPS=1` or `SPLIT_CLASSIFY_N=4` without a stronger replay guard.
- Continue exact-version Code research, but do not replace `v110` unless a public source clearly exceeds the current high-80s branch.

## Next

- [ ] Wait for `v112` and `v113` results.
- [ ] If `v112 >= 89.235`, preserve it as a final-candidate Silver branch.
- [ ] If `v113` scores above `v110`, continue one-knob boundary work around classification latency.
- [ ] If both miss, prefer another `v110` / `v112` exact rerun over risky probe-hop changes.
- [ ] Continue monitoring high-scoring public Code and Discussion for concrete latency-reduction techniques above `89`.
