# Daily Report 2026-07-01

## Competition

- Name: AI Agent Security - Multi-Step Tool Attacks
- Metric: normalized attack score
- Historical best public LB: `54.000` from pre-update `v030_replay_dense_classic600`
- Current confirmed post-update public best: `30.170` from `v043_omega_marker330`

## Today

- Kaggle submission status を確認した。
- `v050_omega_urad_hybrid335` は `SubmissionStatus.COMPLETE` だが public score は空欄。no-score / replay timeout として扱う。
- `v051_omega_pure335` は `SubmissionStatus.COMPLETE` だが public score は空欄。no-score / replay timeout として扱う。
- 公開 notebook `multi-turn-exfiltration-with-adaptive-burst` を分析した。
- `v052_multiturn_hybrid335msg` を提出した。submission ref は `54212725`、現時点では pending。
- `v053_multiturn_conservative250msg` を提出した。submission ref は `54212740`、現時点では pending。
- `v052` / `v053` の結果を確認した。
- `v053_multiturn_conservative250msg` は public `21.500` で完走した。
- `v052_multiturn_hybrid335msg` は public score 空欄で完了したため、no-score / replay timeout として扱う。
- 公開 notebook `ai-agent-security-achieve-and-validate` と `attack-exfil-g1` を分析した。
- `v054_omega_latency_sizer` を提出した。submission ref は `54234607`、現時点では pending。
- `v055_g1_latency_direct` を提出した。submission ref は `54234614`、現時点では pending。

## Results

| Version | Public LB | Status | Notes |
|---|---:|---|---|
| `v055_g1_latency_direct` | pending | pending | attack-exfil-g1 style direct prompt, live latency sizing, floor 300 / cap 520 |
| `v054_omega_latency_sizer` | pending | pending | v043 Omega phrase bank plus live latency sizing, floor 330 / cap 360 |
| `v053_multiturn_conservative250msg` | 21.500 | complete | 150 candidates / 250 messages, lower-risk multi-turn probe; scored but below v043 |
| `v052_multiturn_hybrid335msg` | none | complete / no-score | 215 candidates / 335 messages exceeded the practical replay envelope |
| `v051_omega_pure335` | none | complete / no-score | pure N335 did not preserve v043 success |
| `v050_omega_urad_hybrid335` | none | complete / no-score | URAD hybrid inside v043 envelope did not preserve v043 success |
| `v043_omega_marker330` | 30.170 | complete | current post-update best |

## Notebook Review

- The public multi-turn notebook uses active validation, adaptive burst probing, and fallback logic.
- The safest reusable part is not burst probing but static three-message exfil chains:
  - message 1: `http.post` to unique URL with `SECRET_MARKER`
  - message 2: same shape to another URL
  - message 3: same shape to another URL
- We adopted only static candidates, with no `env.interact()` during attack generation.
- `v053=21.500` shows that multi-turn chains can score, but they did not beat the one-message Omega boundary.
- `v052` no-scored, so total user-message pressure around `335` is too risky in this shape.
- `attack-exfil-g1` contributes a useful live latency probe for candidate-count sizing.
- `ai-agent-security-achieve-and-validate` contributes adaptive validation and replay-safe hard-capping, but its burst branch is risky given our previous multipost failures.
- We turned those ideas into two one-message single-post submissions:
  - `v054`: known-good Omega phrase bank with conservative adaptive expansion.
  - `v055`: higher-risk direct `attack-exfil-g1` style transfer.

## Next

- [x] Check `v052` and `v053` results.
- [x] Build the next candidate around one-message `SECRET_MARKER` exfil, not multi-turn expansion.
- [x] Combine the known-good `v043` Omega phrase mix with `attack-exfil-g1` style live latency sizing.
- [x] Keep a conservative hard cap near the successful boundary first; only then try a high-risk adaptive upper cap.
- [ ] Check `v054` and `v055` results.
