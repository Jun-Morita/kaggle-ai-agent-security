# Daily Report 2026-07-20

## Competition

- Name: AI Agent Security - Multi-Step Tool Attacks
- Deadline: 2026-09-01 final submission deadline
- Metric: normalized attack score
- Current best local replay: deterministic smoke remains `0.00` and is used only for wiring
- Current best public LB: `84.600` from `v093_edgefill_v27_safe982`
- Current best private LB: not visible

## Today

- Confirmed `v095_adaptive_density_ladder_exact` completed with public `73.980`.
- Confirmed `v096_density_ladder_margin150` completed with public `72.675`.
- Identified that the visible `86.175` on `tetsutani/ai-agent-security-adaptive-tool-call-throughput` belongs to version 10, not the later `v136` source used by `v095`.
- Submitted `v097_tetsutani_v10_exact`, the corrected exact port of tetsutani version 10.
- Prepared and submitted `v098_yusuke_v52_exact`, an exact port of Yusuke Another Approach version 52, public reference `85.635`.
- Built and submitted `v099_yusuke_v52_plus_v097_singles`, a controlled hybrid using the `v098` replay ledger plus only `v097` single-post challenger arms.

## Experiments

| Experiment | Hypothesis | Local score | Public LB | Status | Notes |
|---|---|---:|---:|---|---|
| `v097_tetsutani_v10_exact` | Correct public best-version transfer can reproduce around `86.175`. | `0.00` | pending | submitted | Ref `54842840`; source variant `v134_hybrid_single_dual_gate`. |
| `v098_yusuke_v52_exact` | Yusuke version 52 can reproduce around `85.635` and beat `v093`. | `0.00` | pending | submitted | Ref `54842998`; source header `R1-009` / `RELAY PUSH100`. |
| `v099_yusuke_v52_plus_v097_singles` | Adding only `v097` single-post challenger arms to `v098` can find a faster firing template without dual-post replay cost. | `0.00` | pending | submitted | Ref `54843094`; dual-post arms intentionally excluded. |
| `v096_density_ladder_margin150` | Lowering generation reserve from `v095` can improve return count. | `0.00` | `72.675` | complete | Negative evidence; reserve cut hurt. |
| `v095_adaptive_density_ladder_exact` | Later tetsutani `v136` density ladder can reproduce visible public `86.175`. | `0.00` | `73.980` | complete | Negative evidence; visible score belonged to version 10, not this source. |

## Findings

- Public notebook cards can show the best historical version score, not the latest source. Always archive and inspect the specific best-scoring version before exact-porting.
- `v095/v096` failed because they targeted the later `v136` source, while the actual `86.175` source was tetsutani version 10, `v134_hybrid_single_dual_gate`.
- The active strategy is still single-message `SECRET_MARKER` exfiltration with replay-cost-aware throughput control.
- For hybrids, add narrow single-post candidates to a proven replay ledger. Avoid dual-post expansion unless a scored public notebook proves it transfers.

## Decisions

- Keep `v093=84.600` as the current confirmed best until `v097/v098/v099` resolve.
- Treat `v097` as the corrected tetsutani public-best transfer.
- Treat `v098` as the exact Yusuke version 52 fallback.
- Treat `v099` as the controlled hybrid; if it beats `v098`, the useful increment is likely from one of the added single-post arms.

## Next

- [ ] Wait for `v097`, `v098`, and `v099` results.
- [ ] If `v097` wins, promote tetsutani version 10 as the new baseline.
- [ ] If `v098` wins, promote Yusuke version 52 as the new baseline.
- [ ] If `v099` wins, inspect the selected template logs if available and continue single-post arm selection around the winning branch.
- [ ] Avoid spending remaining daily slots until at least one pending result resolves.
