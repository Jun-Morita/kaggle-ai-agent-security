# Daily Report 2026-08-23

## Competition

- Name: AI Agent Security - Multi-Step Tool Attacks
- Current date: 2026-08-23
- Current confirmed post-refresh best: `v166_terminal_say_ok_direct=89.235`

## Status Check

- Kaggle submissions:
  - `v171_sayok_router`: pending, ref `55715068`
  - `v169_replay_costcoef095`: pending, ref `55702321`
  - `v168_terminal_ok_only`: complete, public `86.400`, ref `55701939`
  - `v167_terminal_reply_ok`: complete, public `88.110`, ref `55701934`
  - `v166_terminal_say_ok_direct`: complete, public `89.235`, ref `55693088`
  - `v164_probe_flood_exact`: complete, public `88.830`, ref `55684830`
  - `v165_probe_flood_safe985`: complete, public `87.255`, ref `55684828`
  - `v163_fastfirst_terminal_short`: complete, public `88.920`, ref `55679512`
  - Remaining daily submissions after `v171`: `1`
- Latest leaderboard snapshot:
  - `3926` teams
  - Our team: rank `454`, score `89.235`
  - top-5% boundary: rank `197`, score `91.800`
  - top-10% boundary: rank `393`, score `89.640`

## Public Code

- Latest visible Code list includes:
  - `verityix/ai-agent-security-attack-algorithm-hitherto`: public `45.72`
  - `mccocoful/local-cpu-submission-for-hengck23`: public `2.7`
  - `lopure/jed-multi-step-attack-relay-push100`: public `84.6`
  - `adhirajjagtap/probe-and-flood-strategy`: public `88.695`
  - `evgendvorkin/ai-agent`: public `88.51`
  - `antoniorotundo2/a-throughput-floor-plus-a-genuine-multi-step-searc`: no
    public score returned by score lookup
- No newly checked public Code currently exceeds the current repo best
  `v166=89.235` in a directly transferable way.
- `mccocoful/local-cpu-submission-for-hengck23` is useful as a timing/guardrail
  diagnostic lead, not as an attack template.
- `antoniorotundo2/a-throughput-floor-plus-a-genuine-multi-step-searc` is a
  long source-reading working note around exfil floor, confused-deputy floor,
  and multi-step exploration. Because it has no confirmed public score from the
  score lookup, treat it as analysis material, not an immediate exact-transfer
  candidate.

## Discussions

- Discussion ingest: `2` new, `98` updated in the latest lightweight refresh.
- New / updated practical point from discussion `736246`: a local CPU-style
  setup is being used to reproduce LB timing and private-guardrail timing
  differences. This reinforces that replay throughput, not local GPU
  throughput, is the binding constraint.
- Discussion `736099` remains important: public `SECRET_MARKER` throughput is a
  public-LB lever and may not transfer directly to private guardrails.

## Findings

- `v166=89.235` is the new confirmed post-refresh best. The one-line terminal
  contraction from `Then say OK.` to `Say OK.` is a real positive lever.
- `v167=88.110` and `v168=86.400` are negative readouts: `Reply OK.` and bare
  `OK.` are worse than both `v166` and `v163`.
- `v164=88.830` confirms the public `probe-and-flood-strategy` source transfers
  reasonably, but it does not beat `v166=89.235`.
- `v165=87.255` confirms the `0.985` safety margin is not universally helpful;
  replay-safe settings are source-family-specific.
- `v171` was submitted as a `Say OK.`-centered adaptive terminal router:
  `Say OK.` plain/frame plus `Then say OK.` plain/frame, with the existing six
  classification probes. It is pending as ref `55715068`.
- `v169` is the high-risk / high-return daily branch. It keeps the proven
  `v163` prompt wording and changes only `REPLAY_COST_COEF=1.0 -> 0.95`, trying
  to keep more candidates if the replay cost estimate is conservative.

## Next

- Wait for `v169` and `v171`.
- If `v171` beats `v166`, keep adaptive routing but prune failed arms.
- If `v171` loses, use static `v166` as the anchor and explore only one small
  knob at a time.
- If `v169` beats `v166`, test the same replay-cost optimism on the `Say OK.`
  static anchor.
