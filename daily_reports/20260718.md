# Daily Report 2026-07-18

## Competition

- Name: AI Agent Security - Multi-Step Tool Attacks
- Current confirmed public best: `81.540` from `v092_jed_5tpl_safe098`

## Results

- `v092_jed_5tpl_safe098` completed with public `81.540`.
  It is the new repo best and improves over `v087=76.950` by `+4.590`.
- `v091_jed_5tpl_exact099` completed with public `73.935`.
  It was an exact transfer of the public 84.870 five-template
  `REPLAY_SAFE=0.99` attack code, but scored below `v087` and `v092`.
- `v089_r1_011_push99` and `v090_raw_weighted_push99` completed with blank
  public scores. Treat both as no-score / replay failure.
- `v088_multimessage_m16` had already resolved to public `3.990`, confirming
  that the current multi-message amortization branch is not useful here.

## Notebook Review

- Reviewed public notebook:
  `jed-attack-5-templates-aggressive-replay-0-99.ipynb`
- Public score shown at review time: `84.870`
- Extracted `attack_code` hash: `97a839b8755`
- Key reusable idea:
  compact five-template JED selector with `PROBE_REPS=5`,
  `MIN_FIRE_RATE=0.2`, and all-latency median replay-cost sizing.

## Diagnosis

- `v089` likely failed because it estimated fill cost from successful-fire
  latencies only. That can undercount replay cost and return too many
  candidates.
- `v090` likely failed because the raw-weighted larger template bank increased
  variance near the replay boundary.
- `v091` confirmed that exact `REPLAY_SAFE=0.99` is score-bearing but too
  aggressive for our hosted run.
- `v092` showed that reducing only `REPLAY_SAFE` to `0.98` is the better tradeoff
  for this repo.

## Decision

- Use `v092` as the active baseline.
- Keep `v087` as a safer fallback in the same single-message replay-safe family.
- Continue with small controlled probes around `v092`, especially
  `REPLAY_SAFE` values near `0.98`.
- Do not return to multi-message, broad raw-weighted template banks, or older
  static scaling families unless new public evidence appears.

## Next

- Candidate next submissions:
  - `REPLAY_SAFE=0.975` safety probe from `v092`;
  - `REPLAY_SAFE=0.985` upside probe from `v092`.
- Keep final-submission planning aware that public single-message exfil may not
  fully represent private guardrail behavior.
