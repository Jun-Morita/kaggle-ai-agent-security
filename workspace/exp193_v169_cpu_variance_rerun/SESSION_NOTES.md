# exp193_v169_cpu_variance_rerun

## Hypothesis

The exact `v169` configuration remains the post-refresh public best at 89.685.
Because hosted inference has high variance, sample one additional CPU-wrapper
run without changing attack behavior. This is explicitly a high-risk variance
probe, not evidence that exact reruns are generally reliable.
